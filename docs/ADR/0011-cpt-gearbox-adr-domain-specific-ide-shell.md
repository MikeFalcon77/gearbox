---
status: accepted
date: 2026-08-28
decision-makers: Mike Yastrebtsov
---
# Studio narrows Theia by subtraction and rebinding, and keeps the editor it came for

**ID**: `cpt-gearbox-adr-domain-specific-ide-shell`

## Table of Contents

<!-- generated by `cfs toc` -->

## Context and Problem Statement

Gearbox Studio is a client of the engine, not a code editor, but it is built on Eclipse Theia — and
a stock Theia application is a general-purpose IDE. The slice that exists today shows the problem
plainly: the menu bar carries File, Edit, **Selection**, View, **Go** and Help, of which Selection
comes from `@theia/monaco` and Go from `@theia/editor`
(`node_modules/@theia/editor/lib/browser/editor-menu.js:45`). Neither has anything to do with gears.
Left alone, this drifts further with every dependency added.

The obvious correction — strip Theia down — is wrong, because the editor is precisely why Theia was
chosen over a bespoke UI. Editing `product.gdl` and `gear.gdl` with syntax highlighting, folding,
search and replace, tabs, a file tree and a diff view is a large amount of behaviour nobody wants to
write, and the composition workflow needs all of it.

So the question is how to be domain-specific *and* keep the workbench. That question has a published
answer in a directly comparable product: [Arduino IDE 2.x](https://github.com/arduino/arduino-ide) is
a domain-specific environment on Theia, for a domain (write a sketch, pick a board, verify, upload)
whose shape maps onto ours (write a product, pick a profile, validate, resolve, generate). Its source
is the evidence base for this decision.

## Decision Drivers

* The editor must survive intact. Monaco, tabs, search, the file tree, Git and a terminal stay
  reachable; this is the stated requirement and the reason Theia is here at all.
* What is removed must stay removable. A view hidden by deleting its package cannot be brought back
  by a preference; a view hidden by layout can.
* Every suppression must carry its reason. A one-line `rebind` with no comment is indistinguishable
  from an accident six months later, and there will be dozens of them.
* The mechanism must exist in Theia **1.75**, which is what is pinned. Arduino IDE is on 1.57 and the
  menu model changed in between.
* Nothing may depend on a Theia fork. The moment the shell requires patched Theia, upgrades stop.

## Considered Options

* **A — leave the workbench general.** Add Gearbox views alongside everything Theia provides and let
  the user ignore what is irrelevant.
* **B — subtract by dependency set only.** Choose the `@theia/*` packages precisely; accept whatever
  menus and views the chosen packages contribute.
* **C — subtract by dependency set, then rebind and unregister what remains.** Take B, then override
  Theia's own contributions to remove menu entries, hide views and replace services.
* **D — bypass the workbench.** Use Theia as a shell host but render the product experience in
  custom widgets with a custom layout, treating the editor as a subordinate panel.

## Decision Outcome

**Chosen: C, subtract by dependency set, then rebind and unregister what remains.**

This is what Arduino IDE does, and reading its source settles the question of whether B is
sufficient: it is not. Arduino keeps `@theia/monaco`, `@theia/plugin-ext`, `@theia/debug`,
`@theia/markers`, `@theia/search-in-workspace`, `@theia/outline-view`, `@theia/terminal` and
`@theia/typehierarchy` — the editor stack, entire — and drops `@theia/vsx-registry`,
`@theia/getting-started`, `@theia/task` and `@theia/git`. What it strips is *generality*, not the
editor. Several packages it keeps only because something else needs them, and then explicitly
neuters their presentation. B cannot express that.

The mechanisms, and their state in the version we pin:

| Intent | Arduino's mechanism (1.57) | Available in 1.75 |
|---|---|---|
| Remove a menu entry | `super.registerMenus()`, then `unregisterMenuAction` | Yes; the menu path argument is effectively required, since the default scans the whole tree |
| Remove a whole top-level menu | `getMenu(MAIN_MENU_BAR).removeNode(id)` | No `unregisterSubmenu` exists; `unregisterMenuAction(id, MAIN_MENU_BAR)` reaches a submenu because removal matches on node id |
| Remove a command outright | `CommandRegistry.unregisterCommand` | Yes |
| Hide a view without losing it | `override initializeLayout(): // NOOP` | Yes, and it is also the right hook for *opening* a view |
| Replace a Theia service | `rebind(TheiaX).to(MyX)` | Yes |
| Remove a contribution class | not used by Arduino | `FilterContribution` exists, with no in-tree example |
| A domain toolbar | own `Widget` in shell area `top`, plus `hideTopPanel(): NOOP`, populated from `TabBarToolbarRegistry` | Yes; `isVisible(widget)` scopes an item to a widget |
| Make the workspace root a domain object | `WorkspaceService.toFileStat` override: open a `.ino`, get its sketch folder | Yes |

Two of these deserve emphasis because they are not obvious.

**`initializeLayout(): NOOP` is the correct way to hide a view.** It keeps the package, the command
and the keybinding; only the default layout changes, so a person can still open the view and their
saved layout is respected. The slice already learned the other half of this the hard way: opening a
view from `onStart` runs before the shell is attached, and the panel ends up collapsed while its
widget stays in the DOM — queryable and invisible, which is worse than either.

**`FilterContribution` is available and will not be used by default.** Arduino does not use it; a
filter removes a contribution class wholesale and cannot strip a single menu item, so the fine-grained
work needs `unregisterMenuAction` anyway. Reserved for a contribution that must not run at all.

### The two perspectives

Studio has two objects of work — the gear **catalogue** and a **product** — and neither subordinates
the other. Arduino has only one (the sketch), so it offers no guidance here. The decision is to make
them two switchable perspectives rather than to pick one, with the switch in the toolbar. Which
mechanism carries a perspective is left open: Theia 1.75 has a `PERSPECTIVE_LAYOUTS_STORAGE_KEY` in
`ShellLayoutRestorer`, and if it does not fit, `ApplicationShell.createLayout` is the documented
override point.

### Packaging

Both a browser application and an Electron application, sharing one extension, as Arduino does. The
browser target stays the development and demonstration path because it is faster to iterate and can
be shown over a link; Electron is what a person installs.

### A second, smaller decision, already implemented: the `.gdl` grammar is native

Highlighting `.gdl` could be contributed as a bundled VS Code extension via `theiaPlugins`, which is
what Arduino does for `.ino` and what this project's own plan originally specified. It is not what
was built. The native path — `LanguageGrammarDefinitionContribution` plus `TextmateRegistry` — was
chosen because the VSIX route requires `@theia/plugin-ext` and the whole plugin host in order to
colour one file type in an application whose domain is fixed and whose users are not installing
extensions. Arduino needs the plugin host regardless, for `cortex-debug` and the language server; we
do not.

The consequence worth recording is not the choice but the guard. A grammar is a second place where
the language is written down, so it can disagree with the interpreter. Its volatile half is therefore
generated from the same globals the interpreter evaluates against, and `make grammar-check` fails the
build on drift, exactly as `make ts-check` does for the wire types. The regex craft stays
hand-written, because that part does not change when someone adds a GDL function.

### Scope and boundary

This decision governs the shell: which Theia packages are present, what the menu bar contains, where
views live, and how a view is hidden. It does not decide which Gearbox views exist or what they show
— that is the widget table in the plan — and it does not decide anything about the engine or the
RPC surface.

### Consequences

* **Every `rebind` carries a comment saying why.** Arduino's frontend module is a thousand lines of
  rebinds and is readable only because each one is justified in place. Adopting the mechanism without
  the discipline produces a file nobody can safely change.
* **A contribution base class comes first.** Arduino's `Contribution` implements five Theia
  contribution interfaces at once with the common services pre-injected, and `Contribution.configure`
  binds them in one line. Without it, each feature costs six lines of boilerplate in a shared module,
  which is how that module becomes unreviewable.
* **Upgrading Theia becomes a real task.** Overrides are coupled to Theia's internals, and the menu
  model already changed between 1.57 and 1.75. Mitigated by preferring the documented extension
  points — `initializeLayout`, `TabBarToolbarRegistry`, `createLayout` — over reaching into
  structures, and by the UI check, which fails on a blank screen rather than reporting success.
* **The top panel must be un-hidden to host the toolbar**, which means overriding the method that
  hides it. This is a small piece of coupling accepted deliberately; Arduino does the same.
* **Two perspectives are more surface than one.** Layout state, the switch, and the question of which
  views belong to which perspective all become things to get right. Accepted because neither object
  of work is subordinate.
* **Some Theia surface stays visible on purpose.** Explorer, Terminal and Git are to remain, because
  the workflow ends in generated crates a person will want to build, inspect and diff. This is a
  deliberate divergence from Arduino, which hides more. **Only Explorer is in place today**:
  `@theia/terminal`, `@theia/scm` and `@theia/git` are not dependencies of `browser-app` yet, so the
  other two are decided and unbuilt. Recorded here rather than left implied, because the earlier
  wording claimed all three were present.

### Confirmation

* The UI check asserts what a person sees, not what was registered: rows must have a client
  rectangle, a clicked link must open a tab, and `.gdl` text must be tokenized into more than one
  colour class. A grammar that fails to load renders plain text with no error, and a collapsed panel
  keeps its widget in the DOM — both are silent successes, and both are now failures.
* `ide/tests/conformance/` runs headless against a real application, one test per claim in this ADR
  and in the other documents, and writes `docs/conformance.md`. Every claim above is a row there, so
  a claim this ADR makes and the application does not keep is visible without reading either. The
  number of collected claims is pinned, because a check that silently stops being collected reports a
  smaller denominator as success -- which is what the `if`-guarded script this replaced did.
* `npm ls @theia/core` must report exactly one entry; a second copy breaks inversify identity and is
  the most common Theia build failure.
* Any menu entry this decision removes must be asserted absent, so that a Theia upgrade restoring it
  is caught by a test rather than by a person noticing.

## Pros and Cons of the Options

### A — leave the workbench general

* Good, because there is nothing to build and nothing to maintain against Theia's internals.
* Bad, because the product reads as "an IDE with some gear panels" rather than as a tool for
  composing gears, which is the stated goal.
* Bad, because it degrades with every dependency: adding `@theia/debug` for one feature would add a
  Run menu nobody in this domain wants.

### B — subtract by dependency set only

* Good, because it is the cheapest real mechanism and removes whole feature areas honestly — the
  package is not there, so neither is its menu, its command or its keybinding.
* Good, because it cannot break on a Theia upgrade; there is no coupling to internals.
* Bad, because it cannot reach inside a package that is needed for one thing and contributes five.
  Monaco is required for editing and contributes the Selection menu with it.
* Bad, because some packages arrive transitively regardless.

### C — subtract, then rebind and unregister

* Good, because it can express "keep the feature, drop its presentation", which is most of the work.
* Good, because it is proven at scale in a shipped product on the same framework.
* Good, because `initializeLayout(): NOOP` hides a view without removing the ability to open it,
  which respects the user rather than deciding for them.
* Bad, because it couples the application to Theia's class structure, and that structure moves.
* Bad, because it is easy to accumulate suppressions nobody can justify later; the comment discipline
  is a mitigation, not a fix.

### D — bypass the workbench

* Good, because the result would be exactly the product intended, with no inherited surface.
* Bad, and decisively: it discards the editor, which is the reason Theia is here. Rebuilding tabs,
  search, a file tree and a diff view is not a side quest.
* Bad, because it would still inherit Theia's lifecycle and DI while abandoning the parts that make
  those worth accepting.

## More Information

### What was read

Arduino IDE 2.3.11 on Theia 1.57, at `github.com/arduino/arduino-ide`. Two yarn workspaces —
`arduino-ide-extension` and `electron-app`; no browser target. Overrides live under
`arduino-ide-extension/src/browser/theia/**`, mirroring Theia's own package layout, so an override
sits at the path of the thing it overrides. Menu paths are collected in one
`src/browser/menu/arduino-menus.ts`, which also carries a hand-written `unregisterSubmenu` helper
whose comment calls it "a hack" against unresolved upstream issues, and a `PlaceholderMenuNode` for
permanently-disabled entries. The removal of Selection, Go, Terminal and View happens in four lines
of `src/browser/arduino-frontend-contribution.tsx`; the fine-grained removals happen in
`src/browser/theia/core/common-frontend-contribution.ts`, which calls `super`, then unregisters
eleven commands and sixteen menu actions — note the deliberate split between `unregisterCommand`
(gone entirely) and `unregisterMenuAction` (command and keybinding survive, menu entry does not).

The Electron menu does not update itself when the model changes; Arduino carries a `MainMenuManager`
abstraction whose implementation calls `setMenu(factory.createElectronMenuBar())` after any rebuild.
Anything that builds menus from data needs it.

The one thing Arduino does that this project should copy first is the `Contribution` base class in
`src/browser/contributions/contribution.ts`, with its `onReady()` hook driven off
`FrontendApplicationStateService.reachedState('ready')`.

### The 1.75 facts that differ from 1.57

Verified against the installed packages, not assumed:

* `MenuModelRegistry.unregisterMenuAction` has three overloads and matches on command id; its
  `menuPath` defaults to the root, so omitting it removes every match in the whole tree.
* There is no `unregisterSubmenu`. Removal by node id reaches a submenu, and
  `MutableCompoundMenuNode.removeNode` is the direct route.
* `FrontendApplicationConfig` has no switch for menu-bar or sidebar visibility.
  `window.menuBarVisibility` and `workbench.statusBar.visible` are preferences; the side panels have
  no equivalent, so hiding them means overriding `createLayout`.
* A TextMate grammar needs both `monaco.languages.register({ id })` and
  `mapLanguageIdToTextmateGrammar`. With only the second, activation never fires and there is no
  error — the file renders as plain text.
* `--plugins=local-dir:../plugins`, which the browser application currently passes, does nothing:
  `@theia/plugin-ext` is not installed and the backend's argument parser is not strict, so the flag
  is accepted and ignored. The directory does not exist either.

### When to revisit

* If Studio ever needs to host third-party extensions, `@theia/plugin-ext` arrives and the `.gdl`
  grammar should move to the VSIX path, since the cost that ruled it out will already have been paid.
* If a Theia upgrade removes `initializeLayout` or reworks the menu model again, the suppressions are
  the first thing to re-verify; the assertion that a removed menu stays removed is what will notice.
* If the two perspectives turn out to be one — if in practice nobody uses the catalogue except while
  editing a product — collapse them. That is a cheaper mistake to correct than the reverse.

## Traceability

* Requirements: `cpt-gearbox-fr-studio` (the Studio itself), `cpt-gearbox-fr-editor-diagnostics`
  (diagnostics rendered in the editor, which depends on the editor stack surviving).
* Constrains: `docs/plans/gearbox-builder-prototype.md` §9 (the `ide/` structure, the widget table
  and the Theia traps) and §9.1 (what diverged and why).
* Depends on: ADR `cpt-gearbox-adr-staged-catalogue-loading` — the Catalogue perspective renders
  a staged load, and the shell must not assume a view has all its data when it first appears.
