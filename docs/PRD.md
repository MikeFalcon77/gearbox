# PRD — Gearbox Builder

**System**: `gearbox`
**Status**: draft — written before implementation, to be confirmed by the prototype
**Date**: 2026-08-26

## Table of Contents

<!-- generated by `cfs toc` -->

## 1. Overview

### 1.1 Purpose

Gearbox Builder is the product composition and resolution layer for the Gears platform. It lets a
developer or integrator declare *which* gears belong to a product and *what* deployment shape is
wanted, then deterministically derives the concrete implementation: process topology, local vs
remote contract bindings, transports, cluster provider selections, and the build and deployment
artifacts that realize them.

Key capabilities: a declarative gear/product description language (GDL) hosted in Starlark; a
typed Rust intermediate representation; a deterministic resolver that produces a committed
`product.lock`; generators that emit compilable per-process Cargo crates, Dockerfiles, and a Helm
umbrella chart from that lock; a JSON-RPC engine API; and an explanation graph that answers "why
was this decided" for every automatic choice without invoking a language model.

### 1.2 Background / Problem Statement

The Gears platform already provides gears, contracts, in-process and out-of-process execution,
discovery, runtime wiring, and cluster primitives. What it does not provide is a way to define a
**product**. Today that definition is spread across Cargo features, a hand-maintained
`registered_gears.rs`, YAML runtime config, one hand-written Helm chart, and developer knowledge of
which gears can and cannot be separated. In `gears-rust` today the single example server composes
roughly 25 Cargo features against a 99-line hand-written link file, and the only chart in the
repository serves exactly one gear.

The resulting decisions are made by humans, repeatedly, and are not checkable. Whether a contract
binding is local or remote depends on placement; whether a placement is even legal depends on the
contract's kind; whether a cluster cache satisfies a requirement depends on capabilities that are
declared in Rust but consulted only at startup. Each of these is statically decidable and is
currently decided by hand.

The specific blocker for "one gear per process" is not runtime support — local/remote binding,
out-of-process serving, and directory-based discovery are all implemented. The blocker is that one
binary links every gear, and nobody will hand-write and maintain a dozen application crates. That
is a code-generation problem, and it is the one this system removes.

### 1.3 Goals (Business Outcomes)

- A product's deployment topology can be changed between `embedded`, `host-workers`, and
  `kubernetes` with **zero changes to any gear's business source**, verified by a byte-level diff.
- Every composition decision that is currently made by a human reading ADRs is either derived
  automatically or rejected with an actionable diagnostic.
- An external integrator can compose a product from platform gears plus their own gear **without
  forking the `gears-rust` monorepo**.
- Every automatic decision is explainable by traversing recorded provenance, with no language model
  in the correctness path.
- Any capability the runtime does not support is refused with a citation to the source that proves
  it, rather than being silently mis-generated.

### 1.4 Glossary

| Term | Definition |
|------|------------|
| GDL | Gears Description Language — a constrained Gears domain API hosted in Starlark. Files: `gear.gdl`, `product.gdl`. |
| Catalogue | Developer-owned facts: what gears exist, what they provide, consume, and require. |
| Intent | Operator choices: which gears are enabled, deployment profile, replicas, overrides, preferences. |
| ResolvedProduct | The derived implementation: actual topology, bindings, transports, providers. Serialized as `product.lock`. |
| DeploymentProfile | One of exactly `embedded`, `host-workers`, `kubernetes`. A Gearbox concept, not a runtime type. |
| Preset | A preference overlay such as `dev` or `production`. Orthogonal to DeploymentProfile. Out of scope for this release. |
| ClusterScope | A named cluster coordination scope (`ClusterProfile` in the runtime API). Neither a DeploymentProfile nor a preset. A join key, not free text: it must name a profile the requiring gear's own crate implements, because the runtime resolves it as `ClientScope::new("cluster:{name}")` and a name nothing registered fails only at startup. |
| Projected fact | A catalogue fact read out of Rust rather than declared: gear id, runtime capabilities, co-location dependencies, lifecycle, client trait, contract identity/version/kind, provided and consumed contracts, registered cluster providers (their primitives, names and capabilities), and cluster profile names. A `gear.gdl` restating one is rejected. Read out of *Rust*, not only out of an *attribute*: a provider's name comes from a `PROVIDER_NAME` const and its capabilities from a backend trait impl. |
| Declared fact | A catalogue fact with no Rust home, so it is written in `gear.gdl`: display name, description, category, visibility, `package` (including the `lib` ident), cluster requirements, transport choices, endpoints, criticality, where each cluster plugin crate lives, and whether a cluster backend is process-local or needs credentials. |
| Co-location dependency | `#[toolkit::gear(deps = [...])]`. Link-time: the macro emits a hidden re-export, so the crate is physically in the binary. Never severable by the resolver. A projected fact. |
| Contract consumption | `#[toolkit::consumes(...)]`. A named contract edge whose provider may be local or remote, and which may therefore cross a process boundary. |
| Contract kind | `Api`, `Embedded`, `Backend`, or `Extension`, encoded in the trait-name suffix. `Api` and `Backend` are remote-capable; the others are in-process only. |
| Cut edge | A contract consumption the resolver chose to place across a process boundary, making the binding remote. |
| Selected vs resolved | What the operator asked for versus what the resolver produced. Recorded separately, always. |
| Explanation graph | Provenance nodes and edges recorded *during* resolution, answering "why" for each decision. |

## 2. Actors

> **Note**: Stakeholder needs are managed at project/task level. Documented here are the actors that
> interact with this system.

### 2.1 Human Actors

#### Gear Author

**ID**: `cpt-gearbox-actor-gear-author`

- **Role**: Writes and maintains a gear crate and its `gear.gdl`. Owns the facts about what the gear
  provides, consumes, requires, and can be co-located with.
- **Needs**: To declare those facts once, in one place, and be told immediately when the declaration
  and the code disagree.

#### Integrator

**ID**: `cpt-gearbox-actor-integrator`

- **Role**: Composes a product from platform gears plus their own gear, chooses the deployment
  shape, and deploys the result into an environment they own. May be external to the platform team
  and must not need a fork of the platform monorepo.
- **Needs**: To express what they want and receive a buildable, deployable product; to be told when
  what they want is impossible, and why; to integrate with infrastructure that already exists in
  their cluster.

#### Platform Engineer

**ID**: `cpt-gearbox-actor-platform-engineer`

- **Role**: Owns the platform's composition rules, provider capabilities, and supported topologies.
  Reviews resolved products and decides which contract edges are worth making severable.
- **Needs**: A formal place to encode composition knowledge that currently lives in ADRs and
  memory; an actionable list of the edits that would make a product more separable.

### 2.2 System Actors

#### Gearbox Studio

**ID**: `cpt-gearbox-actor-theia-studio`

- **Role**: The Eclipse Theia client. Browses the catalogue, edits and resolves the product, renders
  graphs and explanations, previews and applies generation. Holds no resolution logic.

#### External AI Agent

**ID**: `cpt-gearbox-actor-external-agent`

- **Role**: A developer's existing coding agent, acting through the engine's typed RPC API rather
  than by editing description files as text. Reads the explanation graph to answer questions and
  proposes intent changes for review. Never part of the correctness path.

#### Cargo

**ID**: `cpt-gearbox-actor-cargo`

- **Role**: Builds the generated process crates and resolves their package dependencies. Owns
  package and build semantics; Gearbox Builder owns product composition and does not duplicate them.

#### Helm

**ID**: `cpt-gearbox-actor-helm`

- **Role**: Consumes the generated chart, validates values against the generated schema, and renders
  manifests. The lowest common layer every downstream deployment tool consumes natively.

## 3. Operational Concept & Environment

Foundational context: [`gears-rust/docs/ARCHITECTURE_MANIFEST.md`](../../gears-rust/docs/ARCHITECTURE_MANIFEST.md)
and [`gears-rust/docs/GEARS.md`](../../gears-rust/docs/GEARS.md). Gearbox Builder has no parent PRD.

### 3.1 Gear-Specific Environment Constraints

- Resolution is **offline and hermetic**: no network access, no clock, no environment reads. The
  resolver is a pure function of Catalogue, Intent, and profile.
- Helm rendering requires **no Kubernetes API access**, so GitOps diffing and offline validation
  work. No `lookup`, no render-time hooks.
- The Catalogue is read from local source trees only. Cargo already materializes `path` and `git`
  sources on disk; registry sources are out of scope for this release.
- Generated process crates path-depend on a `gears-rust` checkout and must build under the
  toolchain that repository pins (`1.97.0`).
- The engine core has **no dependency** on any CLI, RPC, or UI crate. Enforced by an automated
  dependency test, not by convention.

## 4. Scope

### 4.1 In Scope

- GDL evaluation of `gear.gdl` and `product.gdl` into a typed Rust IR, restricted to constructs the
  `gears-rust` runtime actually implements.
- Assembling each gear's descriptor by merging the facts projected from its crate's Rust attributes
  with the facts declared in its `gear.gdl`, and rejecting a `gear.gdl` that restates a projected
  fact (ADR `cpt-gearbox-adr-macro-projected-catalogue`).
- A deterministic resolver covering dependency closure, process partitioning, local/remote binding
  derivation, transport selection, contract version and kind compatibility, cluster capability
  matching, and per-profile structural constraints.
- `product.lock` as the single canonical resolved artifact and the sole input to every generator.
- Generators: per-process Cargo crates with `main.rs` and `registered_gears.rs`, runtime config
  YAML, per-process Dockerfiles, and a Helm umbrella chart with `values.schema.json`.
- An explanation graph and a diagnostic set with actionable remediation text and cited evidence.
- A JSON-RPC engine API over stdio, and a `gearbox` CLI.
- An Eclipse Theia application (Gearbox Studio) as a client of that API.
- All three deployment profiles for a defined vertical slice of real gears plus one new custom gear.

### 4.2 Out of Scope

- MCP adapter, TUI, and Rego/OPA policy evaluation.
- Registry-sourced gears and semantic-version resolution of gear versions.
- Roles, shards, and per-instance addressability as *resolved* concepts — parsed and refused
  (`cpt-gearbox-fr-diagnose-unsupported`).
- Cluster providers beyond those registered in the runtime today.
- SAT/SMT solving and preference scoring beyond deterministic ranking.
- Generating, modifying, or removing any attribute macro in any gear crate. Generated Rust exists
  only in the composition crates Gearbox Builder owns end to end.
- Migration tooling that drafts the declared half of a `gear.gdl`.
- Mass migration of existing gears to declared contract edges.
- Generated Ingress/Gateway resources, bundled database subcharts, ArgoCD ApplicationSets.
- Continuous integration configuration.
- Product presets as a first-class concept.

## 5. Functional Requirements

> **Testing strategy**: All requirements are verified by automated tests, plus the end-to-end
> acceptance procedure in `docs/plans/gearbox-builder-prototype.md` §12. Verification methods are
> documented only where they are non-standard.

### 5.1 Description language (p1)

#### GDL is declarative

- [ ] `p1` - **ID**: `cpt-gearbox-fr-gdl-declarative`

The system **MUST** reject any GDL source containing control flow or a construct that encodes a
decision, including `if`, `for`, `while`, `def`, `lambda`, conditional expressions, comprehensions,
and boolean short-circuit operators, reporting the offending source span.

- **Rationale**: Resolution semantics must live in one place. A conditional in a description file
  moves a decision out of the resolver, destroying determinism, consistency, and explainability.
- **Actors**: `cpt-gearbox-actor-gear-author`, `cpt-gearbox-actor-integrator`

#### GDL exposes only implemented capabilities

- [ ] `p1` - **ID**: `cpt-gearbox-fr-gdl-grounded`

The system **MUST** restrict every enumerated GDL value to the corresponding closed set implemented
in `gears-rust`: runtime capabilities, transports, contract kinds, cluster primitives, cluster
capabilities, and cluster provider names.

- **Rationale**: A description language that can express unimplementable products produces
  configuration that fails at runtime instead of at resolve time.
- **Actors**: `cpt-gearbox-actor-gear-author`

#### GDL evaluation is sandboxed

- [ ] `p1` - **ID**: `cpt-gearbox-fr-gdl-sandbox`

The system **MUST** confine GDL `load()` resolution to the declaring file's source root, reject
parent-directory escapes, and expose no host function that reads the filesystem, environment,
clock, or deployment profile.

- **Rationale**: Hermeticity is what makes repeated evaluation reproducible; it is also what makes
  it impossible for a description file to branch on resolution inputs.

#### Catalogue facts are projected from Rust attributes

- [ ] `p1` - **ID**: `cpt-gearbox-fr-catalogue-projection`

The system **MUST** derive the following from the Rust attributes of each gear crate, and **MUST
NOT** accept any of them as GDL input: the gear id, its runtime capabilities, its co-location
dependencies, its lifecycle, its client trait, the identity, version and kind of every contract it
provides or consumes, and the cluster providers it registers.

- **Rationale**: A fact copied into a second surface drifts whether or not a checker exists. These
  facts are already load-bearing in Rust — the capability list drives compile-time assertions, the
  dependency list emits the re-exports that keep a co-located gear linked — so the attribute is the
  only authority that can be correct by construction.
- **Actors**: `cpt-gearbox-actor-gear-author`, `cpt-gearbox-actor-platform-engineer`
- **Verification Method**: The `--list-gears` and `--list-registered-gears` oracles on the example
  server, compared against the projected catalogue for the slice.

#### A description never restates a projected fact

- [ ] `p1` - **ID**: `cpt-gearbox-fr-gdl-no-restatement`

The system **MUST** reject a `gear.gdl` that declares any fact listed in
`cpt-gearbox-fr-catalogue-projection`, naming both the offending field and the attribute that owns
it.

- **Rationale**: Without an active refusal the mirrored surface returns by accretion, one convenient
  field at a time, and the projection decision decays back into a cross-check.
- **Actors**: `cpt-gearbox-actor-gear-author`
- **Verification Method**: One negative fixture per projected field.

#### The gear attribute is located unambiguously

- [ ] `p1` - **ID**: `cpt-gearbox-fr-attribute-location`

The system **MUST** locate a gear's `#[toolkit::gear]` attribute by scanning its crate's `src/` tree,
**MUST** accept an optional narrowing path on the description's `package` declaration that is
confined to the crate root, and **MUST** report a diagnostic when the scanned path yields zero or
more than one attribute, listing the candidates found.

- **Rationale**: The attribute's location is not uniform — 34 of the 44 in `gears-rust` are at
  `src/gear.rs`, 8 at `src/module.rs`, 2 nested deeper — so a fixed filename would be wrong for ten
  crates. More importantly, one crate may declare several gears: `gears/mini-chat/mini-chat`
  declares three. Projection is meaningless until exactly one attribute is identified.
- **Actors**: `cpt-gearbox-actor-gear-author`
- **Verification Method**: `mini-chat`'s three-gear crate as the positive fixture, plus a zero-match
  and an ambiguous-match negative fixture.

### 5.2 Resolution (p1)

#### Bindings are derived from placement

- [ ] `p1` - **ID**: `cpt-gearbox-fr-derive-binding-from-placement`

The system **MUST** derive whether each contract binding is local or remote from the resolved
placement of consumer and provider, and **MUST NOT** accept a local-or-remote setting as operator
input.

- **Rationale**: This is the central mechanism behind changing topology without changing code. The
  runtime already behaves this way; the resolver must predict it rather than be told.
- **Actors**: `cpt-gearbox-actor-integrator`

#### Co-location dependencies are never severed

- [ ] `p1` - **ID**: `cpt-gearbox-fr-never-cut-colocation`

The system **MUST** treat co-location dependencies as a transitive closure that pulls each
dependency into every process whose closure names it, and **MUST NOT** place a co-location
dependency across a process boundary.

- **Rationale**: The runtime registry treats an absent declared dependency as a hard failure, so a
  severed co-location edge is a product that cannot start. Processes therefore overlap; they are
  not a partition.
- **Verification Method**: Diff the resolved per-process gear list against the binary's own
  inventory-discovered registry output.

#### Undeclared edges are never severed

- [ ] `p1` - **ID**: `cpt-gearbox-fr-never-cut-undeclared-edge`

The system **MUST NOT** place a consumer and provider in different processes unless the dependency
between them is declared as a contract consumption.

- **Rationale**: Direct type-keyed client lookups are widespread and are not all covered by
  declared dependencies. Separating such a pair produces a runtime failure that the resolver has no
  way to foresee. The conservative default reproduces today's behaviour.

#### Severable-if-declared edges are reported

- [ ] `p1` - **ID**: `cpt-gearbox-fr-report-cuttable-if-declared`

For each pair the resolver would be willing to separate if the dependency were declared, the system
**MUST** report the pair together with the exact source edit that would enable it and the file that
edit belongs in.

- **Rationale**: This converts the resolver's refusal into an actionable work list, and is the only
  path by which a real product incrementally becomes separable.
- **Actors**: `cpt-gearbox-actor-platform-engineer`

#### Contract compatibility is checked before deployment

- [ ] `p1` - **ID**: `cpt-gearbox-fr-contract-compatibility`

The system **MUST** report an error when a consumed contract has no provider in the product, when
the provider offers no matching major version, or when an in-process-only contract kind would cross
a process boundary.

- **Rationale**: Each of these is statically decidable and currently surfaces as a startup or
  wiring failure.

#### Transport selection is deterministic and honest

- [ ] `p1` - **ID**: `cpt-gearbox-fr-transport-selection`

The system **MUST** select a transport for each remote binding from the provider's declared
transports using a fixed priority, **MUST** record the operator's selection separately from the
resolved value, and **MUST** report a diagnostic whenever the two differ.

- **Rationale**: Preserving selected-versus-resolved is what makes a downgrade visible instead of
  silent.

#### Cluster requirements are matched against real provider capabilities

- [ ] `p1` - **ID**: `cpt-gearbox-fr-cluster-capability-match`

The system **MUST** resolve each cluster primitive requirement against the capabilities of the
providers actually registered in the runtime, **MUST** report an error when no provider satisfies
the required capabilities, and **MUST** report an error when a process-local provider is selected
for a topology with more than one process or more than one replica.

- **Rationale**: A process-local coordination backend in a multi-process product is a silent
  correctness failure — the runtime starts successfully and elects one leader per replica.
- **Verification Method**: A negative fixture asserting both diagnostics fire together.

#### A cluster requirement names a profile the gear implements

- [ ] `p1` - **ID**: `cpt-gearbox-fr-cluster-profile-join`

The system **MUST** treat a cluster requirement's `profile` as a join key against the requiring
gear's own `impl ClusterProfile { const NAME }`, **MUST** read that name from the constant rather
than deriving it from the marker type's identifier, **MUST NOT** supply a default profile, and
**MUST** report an error naming the profiles the gear does implement when the requirement names one
it does not.

- **Rationale**: The runtime resolves the profile as `ClientScope::new("cluster:{name}")` and a name
  nothing registered fails only at startup, with `ProfileNotBound`. A default would be silently
  wrong for the platform's only production consumer, which binds `"event-broker"`; and deriving the
  name from the marker's identifier would yield `event-broker-profile`, which is wrong for that same
  consumer.
- **Verification Method**: Fixtures reproducing that consumer's real shape — a private marker whose
  `NAME` is not the kebab-case of its identifier — asserting the join succeeds, that renaming `NAME`
  breaks it, and that omitting `profile` is refused rather than defaulted.

#### Per-profile structural constraints are enforced

- [ ] `p1` - **ID**: `cpt-gearbox-fr-profile-constraints`

The system **MUST** enforce the structural constraints of the selected deployment profile,
including single-process composition for `embedded`, the presence of the directory and gRPC hub
gears when directory discovery is used, at most one REST host and one gRPC hub per process, and a
resolvable executable path for each spawned worker.

- **Rationale**: These mirror invariants the runtime enforces at startup or not at all.

#### Resolution is explainable

- [ ] `p1` - **ID**: `cpt-gearbox-fr-explain`

For every automatic choice the system **MUST** record, at the moment the choice is made, the
requirement that forced it, the constraints that eliminated alternatives, the preference that
ranked it, and the source location of each contributing fact; and **MUST** expose that provenance
as a queryable graph.

- **Rationale**: Explainability recorded after the fact is reconstruction, and reconstruction is
  guesswork. Recording during resolution is the only version that stays true.
- **Actors**: `cpt-gearbox-actor-integrator`, `cpt-gearbox-actor-external-agent`

#### Unsupported capabilities are refused with evidence

- [ ] `p1` - **ID**: `cpt-gearbox-fr-diagnose-unsupported`

For each capability described in the product vision but not implemented by the runtime — roles,
shards, per-instance addressability, unregistered cluster providers, gRPC on a severed edge,
registry sources, and Kubernetes-native endpoint resolution — the system **MUST** accept the
declaration, refuse to resolve it, and emit a diagnostic naming the source file and line that
proves the gap.

- **Rationale**: Silently ignoring a declaration produces a product that does not match its
  description. Rejecting it at parse time loses forward compatibility. Accepting and refusing with
  evidence is the only option that is both honest and checkable.
- **Actors**: `cpt-gearbox-actor-integrator`, `cpt-gearbox-actor-platform-engineer`

### 5.3 Artifacts (p1)

#### The lock file is the single resolved artifact

- [ ] `p1` - **ID**: `cpt-gearbox-fr-lock-single-source`

The system **MUST** serialize the resolved product to `product.lock` and **MUST** derive every
generated artifact from that lock alone, without re-deriving any resolution decision.

- **Rationale**: Independent semantic decisions inside a renderer are how a product's Helm output
  and its Cargo output come to disagree.

#### Process crates are generated and compile

- [ ] `p1` - **ID**: `cpt-gearbox-fr-generate-process-crate`

For each resolved process the system **MUST** generate a Cargo manifest, an entry point matching
that process's role, and a link file that keeps every composed gear's registration alive; and the
result **MUST** compile and run.

- **Rationale**: This is the artifact whose absence blocks one-gear-per-process today.
- **Verification Method**: Compile the generated crates, then diff the running binary's own
  registry output against the lock.

#### Runtime configuration is generated

- [ ] `p1` - **ID**: `cpt-gearbox-fr-generate-config`

The system **MUST** generate the runtime configuration for each process, including per-gear runtime
kind and execution settings for spawned workers, endpoint overrides for severed edges, and cluster
scope provider bindings.

- **Rationale**: Topology is expressed to the runtime through configuration; generating the crate
  without the configuration produces a binary that cannot realize the resolved topology.

#### Deployment artifacts are generated

- [ ] `p1` - **ID**: `cpt-gearbox-fr-generate-deployment`

The system **MUST** generate a container build definition per process image and a Helm umbrella
chart with one conditionally enabled subchart per process, covering workload, service,
configuration, and service-account resources.

- **Rationale**: Helm is the lowest layer every downstream deployment tool consumes natively.

#### Values are schema-validated

- [ ] `p1` - **ID**: `cpt-gearbox-fr-values-schema`

The system **MUST** emit a JSON Schema for the generated chart's values that constrains field
presence and type and rejects unknown keys.

- **Rationale**: The resolver knows which fields must exist and of what type; the operator supplies
  the values. A schema is how that split is enforced rather than documented.

#### No secrets in generated values

- [ ] `p1` - **ID**: `cpt-gearbox-fr-no-secrets-in-values`

The system **MUST NOT** write any credential, key, or password into generated values, and **MUST**
express every such input as a reference to an externally managed secret.

- **Rationale**: Placeholder credentials in committed values do not survive enterprise review and
  train operators to commit real ones.
- **Verification Method**: Pattern inspection of all generated values files.

#### Operator-owned files survive regeneration

- [ ] `p1` - **ID**: `cpt-gearbox-fr-preserve-operator-values`

The system **MUST** separate generated values from operator-owned values and **MUST NOT** discard
or overwrite operator edits when regenerating; an unresolvable conflict **MUST** be reported and
the file left untouched.

- **Rationale**: Regeneration that destroys environment-specific values is regeneration nobody runs
  twice.

### 5.4 Interfaces and clients (p2)

#### The engine is reachable over a typed RPC API

- [ ] `p2` - **ID**: `cpt-gearbox-fr-rpc-api`

The system **MUST** expose catalogue loading, product loading, resolution, explanation, lock read
and write, generation planning, preview and application, validation, and graph projection over a
typed JSON-RPC API, returning structured diagnostics rather than formatted text.

- **Rationale**: Wrapping a command-line tool loses typed semantics and structured errors. Every
  client — editor, agent, future TUI — needs the same contract.
- **Actors**: `cpt-gearbox-actor-theia-studio`, `cpt-gearbox-actor-external-agent`

#### Writes are opt-in

- [ ] `p2` - **ID**: `cpt-gearbox-fr-rpc-writes-opt-in`

The system **MUST** refuse every filesystem-mutating RPC method unless the client declared write
capability during initialization, and **MUST** reject any path outside the declared workspace or
source roots.

- **Rationale**: The API's first clients are an editor and an autonomous agent. Read-only by default
  is the only safe posture.

#### Generation is previewable before application

- [ ] `p2` - **ID**: `cpt-gearbox-fr-generate-preview`

The system **MUST** be able to report, without writing anything, the full set of files generation
would touch, the action for each, its ownership class, and its proposed content.

- **Rationale**: Review-then-apply is what makes generation safe to run against a working tree, and
  is the same mechanism a proposal-based agent workflow needs later.

#### A configurator client exists

- [ ] `p2` - **ID**: `cpt-gearbox-fr-studio`

The system **MUST** provide an Eclipse Theia application that browses the catalogue, edits and
resolves a product across profiles, renders the dependency, contract, process, and cluster graphs,
answers "why" for a selected decision, and previews and applies generation — containing no
resolution logic of its own.

- **Rationale**: The engine must be usable by a human without a terminal, and the UI must not become
  a second implementation of product semantics.
- **Actors**: `cpt-gearbox-actor-integrator`, `cpt-gearbox-actor-platform-engineer`

#### Diagnostics reach the editor

- [ ] `p2` - **ID**: `cpt-gearbox-fr-editor-diagnostics`

Description-file diagnostics **MUST** be reported with source ranges over a language-server
interface, and resolution diagnostics **MUST** be surfaced as editor problem markers replaced
atomically on each resolution.

- **Rationale**: A diagnostic nobody sees is a diagnostic nobody acts on; stale markers are worse
  than none.

## 6. Non-Functional Requirements

Baselines: [`gears-rust/docs/ARCHITECTURE_MANIFEST.md`](../../gears-rust/docs/ARCHITECTURE_MANIFEST.md).
Documented below are requirements specific to this system.

### 6.1 Gear-Specific NFRs

#### Deterministic resolution

- [ ] `p1` - **ID**: `cpt-gearbox-nfr-determinism`

Resolving the same catalogue, intent, and profile **MUST** produce a byte-identical `product.lock`,
independent of filesystem enumeration order, map iteration order, or repetition.

- **Threshold**: Byte equality across 1000 randomly permuted input orderings, and across three
  consecutive invocations.
- **Rationale**: The lock is committed and diffed. A lock that churns is a lock nobody reviews, and
  non-determinism would make every downstream artifact unreviewable too.
- **Verification Method**: Property test over permuted inputs, plus a repeat-and-compare shell check.
- **Architecture Allocation**: See DESIGN.md § Component Model — ordered collections throughout the
  IR, content-derived node identifiers, and a canonical serializer.

#### Minimal semantic lock diffs

- [ ] `p2` - **ID**: `cpt-gearbox-nfr-lock-diff-minimal`

Changing one intent value **MUST** produce a lock diff confined to the entries semantically affected
by that change.

- **Threshold**: No incidental reordering or reformatting in the diff.
- **Rationale**: Reviewability is the reason the lock is committed at all.

#### Explainability without a language model

- [ ] `p1` - **ID**: `cpt-gearbox-nfr-explainability`

Every automatic choice recorded in the lock **MUST** be traceable, by graph traversal alone, to the
originating requirement, the provider capability or constraint that admitted it, and the preference
that ranked it.

- **Threshold**: 100% of automatic choices; zero language-model involvement in the correctness or
  explanation path.
- **Rationale**: A generated justification is not evidence. The resolver decides; a model may only
  rephrase what the resolver already recorded.

#### Provenance recorded during resolution

- [ ] `p1` - **ID**: `cpt-gearbox-nfr-provenance-during-resolution`

Provenance **MUST** be written at the moment each decision is made and **MUST NOT** be reconstructed
after resolution completes.

- **Threshold**: No post-resolution inference pass exists in the implementation.
- **Rationale**: Reconstruction infers rationale it did not observe, and drifts from the resolver as
  the resolver changes.

#### Evidence-cited refusals

- [ ] `p1` - **ID**: `cpt-gearbox-nfr-evidence-cited`

Every diagnostic asserting that the runtime does not support something **MUST** carry a file and
line reference in `gears-rust` that substantiates the claim.

- **Threshold**: 100% of unsupported-capability diagnostics.
- **Rationale**: A tool that says "not supported" without proof is indistinguishable from a tool
  with a bug, and gets worked around instead of trusted.

#### Cluster-independent rendering

- [ ] `p1` - **ID**: `cpt-gearbox-nfr-render-without-cluster`

Chart rendering **MUST** succeed with no access to a Kubernetes API server and **MUST NOT** depend
on live cluster state or on non-deterministic generation.

- **Threshold**: Rendering succeeds offline; no cluster-lookup or random-generation construct
  appears in any generated template.
- **Rationale**: Preserves GitOps diffing, offline validation, and reproducibility.

#### Engine independent of its clients

- [ ] `p1` - **ID**: `cpt-gearbox-nfr-engine-has-no-frontend-deps`

The engine **MUST NOT** depend, directly or transitively, on any command-line, RPC, or user
interface crate.

- **Threshold**: Automated dependency assertion over the engine's resolved dependency set.
- **Rationale**: Every client is a peer. A dependency in the wrong direction is how semantics leak
  into a renderer.

#### Client types cannot drift from engine types

- [ ] `p2` - **ID**: `cpt-gearbox-nfr-no-type-drift`

Client-side type definitions for the RPC surface **MUST** be generated from the engine's own types,
and regenerating them **MUST** produce no change.

- **Threshold**: Regeneration is a no-op; an independent schema snapshot also matches.
- **Rationale**: A hand-maintained client mirror of a wire format diverges, and diverges silently.

#### Actionable diagnostics

- [ ] `p2` - **ID**: `cpt-gearbox-nfr-actionable-diagnostics`

Every error-severity diagnostic **MUST** carry a stable code, the structural cause, and at least one
concrete remedy.

- **Threshold**: 100% of error-severity codes, each covered by a fixture.
- **Rationale**: A diagnostic that states a failure without a remedy transfers the work back to the
  reader.

### 6.2 NFR Exclusions

- **Availability and uptime**: not applicable — this is a build-time tool with no running service.
- **Multi-tenancy and authorization**: not applicable — the tool runs with the invoking developer's
  own filesystem permissions.
- **Horizontal scalability**: not applicable — single-process, single-workspace.
- **Resolution latency**: explicitly not a requirement for this release. Correctness and determinism
  take precedence; generated-crate build time is dominated by Cargo and is out of the tool's control.

## 7. Public Library Interfaces

### 7.1 Public API Surface

#### GDL

- [ ] `p1` - **ID**: `cpt-gearbox-interface-gdl`

- **Type**: Description language (Starlark-hosted domain API)
- **Stability**: experimental
- **Description**: The `gear.gdl` and `product.gdl` vocabulary — the surface gear authors and
  integrators write against.
- **Breaking Change Policy**: Free to change during this release; every change must be accompanied
  by an updated slice and a diagnostic for the removed construct.

#### `product.lock`

- [ ] `p1` - **ID**: `cpt-gearbox-interface-product-lock`

- **Type**: Data format (TOML, carrying an explicit schema version)
- **Stability**: unstable
- **Description**: The canonical resolved product. Committed, diffable, and the sole input to every
  generator.
- **Breaking Change Policy**: Schema version increment; the reader must reject an unknown version
  rather than guess.

#### Engine RPC API

- [ ] `p2` - **ID**: `cpt-gearbox-interface-rpc`

- **Type**: Protocol (JSON-RPC 2.0 over stdio, length-prefixed framing)
- **Stability**: experimental
- **Description**: The engine's typed API, shared by the editor client, the language client, and any
  future agent adapter.
- **Breaking Change Policy**: Client types are generated from engine types; a breaking change must
  fail the generation-is-a-no-op check.

#### `gearbox` CLI

- [ ] `p2` - **ID**: `cpt-gearbox-interface-cli`

- **Type**: CLI (standalone binary, not a Cargo subcommand)
- **Stability**: experimental
- **Description**: `validate`, `catalogue`, `resolve`, `explain`, `lock`, `generate`, `rpc`.
- **Breaking Change Policy**: Machine-readable output is the stable contract; human-readable output
  is not.

### 7.2 External Integration Contracts

#### Gear description contract

- [ ] `p1` - **ID**: `cpt-gearbox-contract-gear-gdl`

- **Direction**: required from the gear author
- **Protocol/Format**: `gear.gdl` beside the gear's `#[toolkit::gear]` attribute — at the crate root
  for a single-gear crate, in the attribute's own directory for a gear nested inside one
- **Compatibility**: A gear participates in product composition if and only if this file exists,
  resolves to exactly one `#[toolkit::gear]`, and declares no projected fact. Agreement with the
  attributes is not a criterion: the overlapping facts are read from the attributes, so there is
  nothing left to agree about.

#### Runtime configuration contract

- [ ] `p1` - **ID**: `cpt-gearbox-contract-runtime-config`

- **Direction**: provided to the Gears runtime
- **Protocol/Format**: the runtime's own configuration schema (YAML)
- **Compatibility**: Generated configuration must deserialize into the runtime's existing
  configuration types unchanged. Gearbox Builder introduces no new runtime configuration keys.

#### Chart values contract

- [ ] `p1` - **ID**: `cpt-gearbox-contract-chart-values`

- **Direction**: provided to the operator
- **Protocol/Format**: Helm values plus a JSON Schema
- **Compatibility**: Generated and operator-owned values are separate layers; the schema is the
  compatibility statement between them.

## 8. Use Cases

#### Switch deployment profile without touching gear code

- [ ] `p1` - **ID**: `cpt-gearbox-usecase-switch-profile`

**Actor**: `cpt-gearbox-actor-integrator`

**Preconditions**:
- A product description selects a custom gear and several platform gears.
- Each selected gear has a valid description that resolves to exactly one `#[toolkit::gear]`.

**Main Flow**:
1. The integrator resolves the product for `embedded` and generates artifacts.
2. The generated single-process binary builds and serves every composed gear's interface, with all
   contract bindings local.
3. The integrator changes only the profile selection and resolves again for `host-workers`.
4. The resolver partitions the product, derives the severed edge as remote, and selects a transport.
5. Generation emits one crate per process plus the spawn and endpoint configuration; the host starts
   and the worker registers and becomes ready.
6. The integrator repeats for `kubernetes` and obtains images and a chart.

**Postconditions**:
- Three topologies exist, produced from one description.
- No gear's source tree changed by a single byte.

**Alternative Flows**:
- **The requested separation is not permitted**: resolution reports which dependency blocks it, why,
  and the exact edit that would permit it; no artifacts are written.

#### Explain an automatic provider choice

- [ ] `p2` - **ID**: `cpt-gearbox-usecase-explain-provider-choice`

**Actor**: `cpt-gearbox-actor-platform-engineer`

**Preconditions**: A resolved product in which at least one cluster provider was chosen
automatically.

**Main Flow**:
1. The engineer asks why a given cluster primitive resolved to its provider.
2. The system returns the requirement that demanded it, the capabilities that admitted the provider
   and excluded the alternatives, the preference that ranked it, and the source location of each
   fact.

**Postconditions**: The engineer can verify every step against the cited source.

#### Diagnose an invalid topology

- [ ] `p1` - **ID**: `cpt-gearbox-usecase-diagnose-invalid-topology`

**Actor**: `cpt-gearbox-actor-integrator`

**Preconditions**: A product requesting a topology the runtime cannot realize — for example a
process-local coordination backend with several replicas, or a required capability no provider has.

**Main Flow**:
1. The integrator resolves the product.
2. The system reports each violation with its structural cause, a per-candidate capability
   comparison, cited evidence, and the supported alternatives.
3. No lock is written and no artifact is generated.

**Postconditions**: The invalid product never reaches a cluster.

## 9. Acceptance Criteria

Each criterion corresponds to a step of the acceptance procedure in
[docs/plans/gearbox-builder-prototype.md](./plans/gearbox-builder-prototype.md) §12.

- [ ] Every gear in the slice validates clean against its own Rust source (§12 step 1).
- [ ] The `embedded` profile builds, runs, serves both interfaces, and its running binary's own
      registry output matches the lock (§12 step 2).
- [ ] The `host-workers` profile produces two processes; the host spawns the worker, the worker
      becomes ready, and the severed edge resolves remotely (§12 step 3).
- [ ] The `kubernetes` profile renders offline, uses no cluster-dependent construct, contains no
      secret in any values file, carries endpoint wiring in configuration rather than environment,
      and rejects schema-invalid values (§12 step 4).
- [ ] An unsatisfiable capability requirement produces both the capability and the topology
      diagnostic, with evidence, and writes nothing (§12 step 5).
- [ ] Three consecutive resolutions are byte-identical, and a second generation writes nothing
      (§12 step 6).
- [ ] Across all three profiles, no gear's source is modified, verified by a byte-level diff
      (§12 step 7).
- [ ] Regenerating client types is a no-op (§12 step 8).
- [ ] The Studio browses the catalogue, switches profiles, surfaces the profile-specific
      diagnostics, renders the process graph, answers "why" for a severed edge, and preserves a
      hand-edited operator values file across regeneration (§12 step 9).

## 10. Dependencies

| Dependency | Description | Criticality |
|------------|-------------|-------------|
| `gears-rust` checkout | Source of the catalogue and the path dependencies of every generated crate | p1 |
| Gears runtime registry and lifecycle | Composition, topological ordering, and the co-location invariant the resolver models | p1 |
| Gears contract binding and discovery | Local/remote wiring and endpoint resolution the resolver predicts | p1 |
| Gears cluster subsystem | The capability and provider model resolution matches against | p1 |
| Cargo + pinned Rust toolchain | Builds generated process crates | p1 |
| Starlark evaluator (Rust) | GDL runtime | p1 |
| Helm | Renders and validates generated charts | p1 |
| Docker | Builds process images | p2 |
| Eclipse Theia | Host application for the configurator client | p2 |
| PostgreSQL | The only cross-process cluster backend that exists; required for multi-process profiles | p2 |

## 11. Assumptions

- Co-location dependencies reflect reality: a declared dependency corresponds to real client usage.
  *Validate by comparing declared dependencies against actual client lookups for a sample of gears.*
- Severing one contract edge is a local, reversible source change that leaves other consumers of the
  same provider untouched. *This is the assumption the whole separation story rests on; validate
  first.*
- A newly generated gear is born with declared contract edges, so the conservative default is needed
  only for pre-existing gears.
- With projection, two former assumptions become hard dependencies of catalogue loading rather than
  things that would merely be convenient:
  - Contract version compatibility is determined from source attributes alone, without package
    metadata, and therefore keeps working for pinned-revision sources.
  - Parsing gear source from a fetched revision yields the same catalogue as parsing a working tree.

  A `syn` parse failure is consequently a hard catalogue error, not a warning.
- With only two cluster providers, validity checking is worth more than preference scoring.
  *Validate by counting how many real decisions have more than one valid candidate; fewer than three
  means scoring is not yet warranted.*
- A generated chart is acceptable in a cluster the integrator does not own, given external database
  and externally managed secret support.
- An external integrator is the target user shape; if none exists yet, an internal platform engineer
  is an adequate proxy.

## 12. Risks

| Risk | Impact | Mitigation |
|------|--------|------------|
| Co-location closure swallows the product, so per-gear placement collapses to one process | The resolver is correct but useless; the central promise is undemonstrable | Choose a slice with at least one genuinely severable declared edge; report severable-if-declared pairs as an actionable list (`cpt-gearbox-fr-report-cuttable-if-declared`) |
| The description language duplicates facts already in code and drifts from them | `gear.gdl` stops being trustworthy | Structurally prevented: overlapping facts are projected from the attributes, and a description restating one is rejected (`cpt-gearbox-fr-catalogue-projection`, `cpt-gearbox-fr-gdl-no-restatement`) |
| Catalogue loading now depends on parsing Rust successfully, so a `syn` failure or an unlocatable attribute blocks resolution entirely | No catalogue, no product; the failure is total rather than degraded | Accepted deliberately (ADR `cpt-gearbox-adr-macro-projected-catalogue`); the attribute locator reports candidates and the exact narrowing line to add (`cpt-gearbox-fr-attribute-location`) |
| Runtime semantics are narrower than the vision assumes | Generated products that cannot run | Ground every resolver rule in a cited runtime capability; refuse the rest with evidence (`cpt-gearbox-fr-diagnose-unsupported`) |
| The cluster subsystem is not composed into any running product today | Integration friction concentrated in one milestone | The custom gear in the slice is deliberately its first consumer; budget a milestone-sized slip there |
| GDL grows into a general-purpose language | Determinism and explainability lost | Small frozen host API, dialect lockdown, and a construct blacklist (`cpt-gearbox-fr-gdl-declarative`) |
| Too much solver too early | Complexity with no payoff while only two providers exist | Valid/invalid plus a single deterministic ranking pass; no search, no scoring |
| Build and product versioning are conflated | Unclear ownership of resolution | Cargo owns package and build resolution; Gearbox Builder owns product composition; the lock pins the product |
| The description language runtime is pre-1.0 and its API moves | Churn on every dependency bump | Pin exactly; confine every reference to a single crate behind one facade type |
| Generated crates and their source repository diverge in toolchain | Silent build differences | Pin the generated workspace to the same toolchain as the source repository |

## 13. Open Questions

- Does a co-location dependency need a "remote-capable" marker, so that severing an edge requires no
  edit in the consumer at all?
- How should the catalogue handle a gear whose declared contract edges depend on a build feature the
  resolver itself selects — a cyclic dependency between catalogue and intent?
- Should validation report gears with no declared dependencies at all as opaque, or is that noise?
- One image per process, or one image with several entry points? Separate images are honest about
  size; a single image is simpler to build and publish.
- Where do the engine crates ultimately live, and how are they versioned relative to the runtime
  whose behaviour they model?
- Does the deployment profile eventually become a runtime type, or does it stay a
  composition-time-only concept?
- Which gear templates does an integrator need, given that a template is the only place declared
  contract edges appear without manual work?

## 14. Traceability

- **Design**: [DESIGN.md](./DESIGN.md) — written after the prototype, per
  [plans/gearbox-builder-prototype.md](./plans/gearbox-builder-prototype.md) §14
- **ADRs**: [ADR/](./ADR/)
- **Plan**: [plans/gearbox-builder-prototype.md](./plans/gearbox-builder-prototype.md)
- **Vision**: [gearbox-builder-vision.md](./gearbox-builder-vision.md)
