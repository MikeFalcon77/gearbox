apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "<< name >>.fullname" . }}-config
  labels:
    {{- include "<< name >>.labels" . | nindent 4 }}
data:
  << config_filename >>: |
<< config_yaml >>
