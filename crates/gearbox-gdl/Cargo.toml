[package]
name = "gearbox-gdl"
description = "Gears Description Language: a constrained Starlark-hosted domain API evaluated into the Gearbox typed model."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true
license.workspace = true

[lints]
workspace = true

[dependencies]
gearbox-ir.workspace = true
serde_json.workspace = true
serde.workspace = true
thiserror.workspace = true
anyhow.workspace = true

# The Starlark host. Pinned exactly: this crate is pre-1.0 and 0.14 was a hard
# break from 0.13 (private `Module::new`, `Heap<'v>` by value, a third Evaluator
# lifetime, a mandatory `StarlarkPagable` derive). Nothing outside this crate
# may name a `starlark::` type, so an upgrade is contained here.
starlark.workspace = true
starlark_syntax.workspace = true
allocative.workspace = true
